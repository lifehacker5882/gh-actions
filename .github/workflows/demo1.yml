name: Events Demo 1
on:
  pull_request:
    types:
      - opened
    branches:
      - main
      - "dev-*" # dev-new dev-this-is-new
      - "feat/**" # feat/new feat/new/button
  workflow_dispatch:
  push:
    branches:
      - main
      - "dev-*" # dev-new dev-this-is-new
      - "feat/**" # feat/new feat/new/button
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output event data
        run: echo "${{ toJSON(github.event) }}"
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
        working-directory: ./third-action
      - name: Test code
        run: npm run test
        working-directory: ./third-action
      - name: Build code
        run: npm run build
        working-directory: ./third-action
      - name: Deploy project
        run: echo "Deploying..."
